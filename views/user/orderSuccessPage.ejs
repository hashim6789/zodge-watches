<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order Success</title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        font-family: "Poppins", sans-serif;
        background-color: #f4f4f4;
      }

      .container {
        text-align: center;
        background: #fff;
        padding: 40px;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .checkmark-circle {
        width: 100px;
        height: 100px;
        margin: 0 auto;
        border-radius: 50%;
        background-color: #4caf50;
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .circle-background {
        width: 100%;
        height: 100%;
        background-color: #4caf50;
        border-radius: 50%;
        position: absolute;
        top: 0;
        left: 0;
        animation: pop 0.4s ease-in-out;
      }

      .checkmark {
        width: 50px;
        height: 25px;
        border-radius: 5px;
        border: solid white;
        border-width: 0 0 6px 6px;
        transform: rotate(-45deg);
        position: absolute;
        animation: draw 1s ease-in-out forwards;
      }

      h1 {
        font-size: 2em;
        margin: 20px 0;
        color: #333;
      }

      p {
        font-size: 1.2em;
        color: #666;
      }

      .order-id {
        font-weight: bold;
        color: #333;
      }

      .home-btn {
        display: inline-block;
        margin-top: 20px;
        padding: 10px 20px;
        background-color: #4caf50;
        color: #fff;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s ease;
      }

      .home-btn:hover {
        background-color: #45a049;
      }

      @keyframes pop {
        0% {
          transform: scale(0.5);
        }
        100% {
          transform: scale(1);
        }
      }

      @keyframes draw {
        0% {
          width: 0;
          height: 0;
        }
        50% {
          width: 50px;
          height: 0;
        }
        100% {
          width: 50px;
          height: 25px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="checkmark-circle">
        <div class="circle-background"></div>
        <div class="checkmark draw"></div>
      </div>
      <h1>Order Placed Successfully!</h1>
      <p>Your order has been placed successfully.</p>
      <p class="order-id">
        Order ID: <span id="order-id"><%= orderId %></span>
      </p>
      <a href="/user/auth/home" class="home-btn">Continue Shopping</a>
    </div>
    <script src="script.js"></script>
  </body>
</html>
