<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Shoping Cart</title>
    <%- include('partials/_header') %>
  </head>
  <body class="animsition">
    <!-- Header -->
    <%- include('partials/_navbar', {user, wishlist, cart}) %>

    <!-- Wishlist -->
    <%- include('partials/_wishlist', {wishlist}) %>

    <!-- Cart -->

    <!-- breadcrumb -->
    <div class="container pt-5">
      <div class="bread-crumb flex-w p-l-25 p-r-15 p-t-30 p-lr-0-lg">
        <a href="/" class="stext-109 cl8 hov-cl1 trans-04">
          Home
          <i class="fa fa-angle-right m-l-9 m-r-10" aria-hidden="true"></i>
        </a>

        <span class="stext-109 cl4"> Shoping Cart </span>
      </div>
    </div>
    <% console.log(cart.products[0])%>

    <!-- Shopping Cart -->
    <form class="bg0 p-t-75 p-b-85" action="/cart" method="post">
      <div class="container">
        <div class="row">
          <div class="col-lg-10 col-xl-7 m-lr-auto m-b-50">
            <div class="m-l-25 m-r--38 m-lr-0-xl">
              <% if (cart.products.length > 0) { %>
              <!-- Cart is not empty, show cart items -->
              <div class="wrap-table-shopping-cart">
                <table class="table-shopping-cart">
                  <tr class="table_head">
                    <th class="column-1">Product</th>
                    <th class="column-2"></th>
                    <th class="column-3">Price</th>
                    <th class="column-4">Quantity</th>
                    <th class="column-5">Total</th>
                    <th class="column-6">Remove</th>
                  </tr>

                  <% cart.products.forEach(function(product) { %>
                  <tr
                    class="table_row"
                    data-product-id="<%= product.productId._id %>"
                  >
                    <td class="column-1">
                      <div class="how-itemcart1">
                        <button
                          id="cartProduct"
                          data-product-id="<%= product.productId._id %>"
                        >
                          <img
                            src="/public/uploads/<%= product.productId.images[0] %>"
                            alt="IMG"
                          />
                        </button>
                      </div>
                    </td>
                    <td class="column-2"><%= product.productId.name %></td>
                    <td class="column-3">₹ <%= product.price.toFixed(2) %></td>
                    <td class="column-4">
                      <div class="wrap-num-product flex-w m-l-auto m-r-0">
                        <div
                          class="btn-num-product-down cl8 hov-btn3 trans-04 flex-c-m"
                          onclick="updateQuantity('<%= product.productId._id %>', -1, '<%= product.productId.stock %>')"
                        >
                          <i class="fs-16 zmdi zmdi-minus"></i>
                        </div>
                        <input
                          class="mtext-104 cl3 txt-center num-product"
                          type="number"
                          name="num-product<%= product.productId._id %>"
                          value="<%= product.quantity %>"
                          data-stock="<%= product.productId.stock %>"
                          disabled
                        />
                        <div
                          class="btn-num-product-up cl8 hov-btn3 trans-04 flex-c-m"
                          onclick="updateQuantity('<%= product.productId._id %>', 1, '<%= product.productId.stock %>')"
                        >
                          <i class="fs-16 zmdi zmdi-plus"></i>
                        </div>
                      </div>
                      <div
                        id="feedback-<%= product.productId._id %>"
                        class="invalid-feedback"
                        style="display: none; color: red"
                      >
                        Quantity exceeds available stock.
                      </div>
                    </td>
                    <td class="column-5">
                      ₹
                      <span id="totalPrice-<%= product.productId._id%>">
                        <%= (product.price * product.quantity).toFixed(2) %>
                      </span>
                    </td>
                    <td class="column-6">
                      <button
                        class="btn-delete-product"
                        onclick="removeFromCart('<%= product.productId._id %>')"
                        title="Remove item"
                      >
                        <i class="zmdi zmdi-delete p-3 fs-20"></i>
                      </button>
                    </td>
                  </tr>
                  <% }) %>
                </table>
              </div>
              <% } else { %>
              <div class="text-center">
                <p class="mtext-103 cl2">Your cart is currently empty.</p>
                <a
                  href="/"
                  class="flex-c-m stext-101 cl2 size-116 bg8 bor14 hov-btn3 p-lr-15 trans-04 pointer m-tb-10"
                  >Continue Shopping</a
                >
              </div>
              <% } %>
              <!-- Remaining cart details -->
            </div>
          </div>

          <!-- Cart totals section -->
          <% if (cart.products.length > 0) { %>
          <div class="col-sm-10 col-lg-7 col-xl-5 m-lr-auto m-b-50">
            <div
              class="bor10 p-lr-40 p-t-30 p-b-40 m-l-63 m-r-40 m-lr-0-xl p-lr-15-sm"
            >
              <h4 class="mtext-109 cl2 p-b-30">Cart Totals</h4>
              <!-- Subtotal -->
              <div class="flex-w flex-t bor12 p-b-13">
                <div class="size-208">
                  <span class="stext-110 cl2"> Subtotal: </span>
                </div>
                <div class="size-209">
                  <span id="subtotal" class="mtext-110 cl2">
                    ₹ <%= cart.products.reduce((acc, product) => acc +=
                    (product.price * product.quantity), 0).toFixed(2) %>
                  </span>
                </div>
              </div>

              <!-- Coupon Discount Field -->
              <div class="flex-w flex-t p-t-15 p-b-13">
                <div class="size-208">
                  <span class="stext-110 cl2"> Coupon Discount: </span>
                </div>
                <div class="size-209">
                  <span id="couponDiscountAmount" class="mtext-110 cl2"> </span>
                </div>
              </div>

              <!-- Total -->
              <div class="flex-w flex-t p-t-27 p-b-33">
                <div class="size-208">
                  <span class="mtext-101 cl2"> Total: </span>
                </div>
                <div class="size-209 p-t-1">
                  <span id="totalAmount" class="mtext-110 cl2">
                    ₹ <%= (cart.totalPrice).toFixed(2) %>
                  </span>
                </div>
              </div>
              <button
                class="flex-c-m stext-101 cl0 size-116 bg3 bor14 hov-btn3 p-lr-15 trans-04 pointer"
              >
                Proceed to Checkout
              </button>
            </div>
          </div>
          <% } %>
          <div class="flex-w flex-sb-m bor15 p-t-18 p-b-15 p-lr-40 p-lr-15-sm">
            <div class="flex-w flex-m m-r-20 m-tb-5">
              <input
                id="couponCode"
                class="stext-104 cl2 plh4 size-117 bor13 p-lr-20 m-r-10 m-tb-5"
                type="text"
                name="coupon"
                placeholder="Coupon Code"
              />
              <button
                class="flex-c-m stext-101 cl2 size-118 bg8 bor13 hov-btn3 p-lr-15 trans-04 pointer m-tb-5"
                id="applyCouponBtn"
                type="button"
              >
                Apply coupon
              </button>
            </div>

            <button
              class="flex-c-m stext-101 cl2 size-119 bg8 bor13 hov-btn3 p-lr-15 trans-04 pointer m-tb-10"
              type="button"
              onclick="removeCoupon()"
            >
              Remove Coupon
            </button>
          </div>

          <!-- Add a section to display the coupon discount, if applied -->
          <div
            id="couponFeedback"
            class="stext-101 cl2 m-tb-10"
            style="display: none"
          >
            Coupon applied! Discount: ₹ <span id="couponDiscount"></span>
          </div>

          <!-- Error message for invalid coupon -->
          <div
            id="invalidCouponFeedback"
            class="invalid-feedback"
            style="display: none; color: red"
          >
            Invalid or expired coupon code.
          </div>

          <!-- Hidden field to store discount amount -->
          <input type="hidden" id="discountAmount" value="0" />
        </div>
      </div>
    </form>

    <!-- Footer -->
    <%- include('partials/_footer') %>

    <!-- Back to top -->
    <div class="btn-back-to-top" id="myBtn">
      <span class="symbol-btn-back-to-top">
        <i class="zmdi zmdi-chevron-up"></i>
      </span>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
      // Attach event listener for Apply Coupon button
      document
        .getElementById("applyCouponBtn")
        .addEventListener("click", function () {
          const couponCode = document.getElementById("couponCode").value;

          // Send the coupon code to the server using Axios
          if (couponCode) {
            axios
              .post("/cart/coupon/apply", { couponCode })
              .then((response) => {
                if (response.data.success) {
                  console.log("testing");
                  // If the coupon is valid, update the cart total and show the discount
                  const discount = response.data.discountAmount;
                  document.getElementById("couponDiscount").innerText =
                    discount.toFixed(2);
                  document.getElementById("couponFeedback").style.display =
                    "block";
                  document.getElementById(
                    "couponDiscountAmount"
                  ).innerText = `₹ ${discount.toFixed(2)}`;

                  // Update the total price with the discount
                  // const subtotalElement = document.getElementById("subtotal");
                  // const originalTotal = parseFloat(
                  //   subtotalElement.innerText.replace("₹ ", "")
                  // );
                  // const newTotal = originalTotal - discount;
                  // subtotalElement.innerText = `₹ ${newTotal.toFixed(2)}`;

                  // // Store the discount amount in a hidden field for further use
                  // document.getElementById("discountAmount").value = discount;
                } else {
                  // Show invalid coupon message
                  document.getElementById(
                    "invalidCouponFeedback"
                  ).style.display = "block";
                }
              })
              .catch((error) => {
                console.error(error);
                // Handle errors like network issues
                alert(error.response.data.message);
              });
          } else {
            alert("Please enter a coupon code.");
          }
        });

      // Function to update the cart
      function removeCoupon() {
        // Reset the coupon code input field
        document.getElementById("couponCode").value = "";

        // Remove the applied discount (set to 0)
        const discount = 0;

        // // Get the current subtotal from the cart
        // const subtotalElement = document.getElementById("subtotal");
        // const originalTotal = parseFloat(subtotalElement.innerText.replace("₹ ", ""));

        // Since there's no discount, the new total will be the same as the original total
        // const newTotal = originalTotal - discount;

        // Update the total amount element with the original total price
        // const totalElement = document.getElementById("totalAmount");
        // totalElement.innerText = `₹ ${newTotal.toFixed(2)}`;

        // Optionally, reset any hidden discount field to 0
        document.getElementById("discountAmount").value = discount;

        // Show a success message for the coupon removal
        alert("Coupon removed successfully!");
      }

      //   document.getElementById(
      //     "couponDiscountAmount"
      //   ).innerText = `₹ ${discount.toFixed(2)}`;

      //   // Calculate the new total by subtracting the discount
      //   const newTotal = originalTotal - discount;

      //   // Update the subtotal element with the new total price
      //   subtotalElement.innerText = `₹ ${newTotal.toFixed(2)}`;

      //   // Store the discount amount in a hidden field for further use
      //   // document.getElementById("discountAmount").value = discount;

      //   // Send the updated cart details to the server (placeholder)
      //   alert("Cart updated successfully!");

      //   // Here you can add logic to send updated cart details to the server
      // }
    </script>

    <!-- <script>
      // Function to apply the coupon code
      function applyCoupon() {
        const couponCode = document.getElementById("couponCode").value;
        const feedback = document.getElementById("couponFeedback");

        if (!couponCode) {
          feedback.textContent = "Please enter a coupon code.";
          feedback.className = "alert alert-danger";
          feedback.style.display = "block";
          return;
        }

        axios
          .post("/apply-coupon", { coupon: couponCode })
          .then((response) => {
            if (response.data.success) {
              feedback.textContent = "Coupon applied successfully!";
              feedback.className = "alert alert-success";
              feedback.style.display = "block";
              updateCartTotals(response.data.cart); // Update cart totals with new data
            } else {
              feedback.textContent =
                response.data.message || "Failed to apply coupon.";
              feedback.className = "alert alert-danger";
              feedback.style.display = "block";
            }
          })
          .catch((error) => {
            feedback.textContent = "An error occurred. Please try again.";
            feedback.className = "alert alert-danger";
            feedback.style.display = "block";
          });
      }

      // Function to remove the applied coupon
      function removeCoupon() {
        axios
          .post("/remove-coupon")
          .then((response) => {
            if (response.data.success) {
              document.getElementById("couponCode").value = "";
              document.getElementById("couponFeedback").textContent =
                "Coupon removed successfully!";
              document.getElementById("couponFeedback").className =
                "alert alert-success";
              document.getElementById("couponFeedback").style.display = "block";
              updateCartTotals(response.data.cart); // Update cart totals with new data
            } else {
              document.getElementById("couponFeedback").textContent =
                response.data.message || "Failed to remove coupon.";
              document.getElementById("couponFeedback").className =
                "alert alert-danger";
              document.getElementById("couponFeedback").style.display = "block";
            }
          })
          .catch((error) => {
            document.getElementById("couponFeedback").textContent =
              "An error occurred. Please try again.";
            document.getElementById("couponFeedback").className =
              "alert alert-danger";
            document.getElementById("couponFeedback").style.display = "block";
          });
      }

      // Function to update the cart totals
      function updateCartTotals(cart) {
        document.getElementById(
          "subtotal"
        ).textContent = `₹ ${cart.totalPrice.toFixed(2)}`;
        document.getElementById(
          "total"
        ).textContent = `₹ ${cart.totalPrice.toFixed(2)}`;
      }
    </script> -->

    <!--===============================================================================================-->
    <script src="/public/user/vendor/jquery/jquery-3.2.1.min.js"></script>
    <!--===============================================================================================-->
    <script src="/public/user/vendor/animsition/js/animsition.min.js"></script>
    <!--===============================================================================================-->
    <script src="/public/user/vendor/bootstrap/js/popper.js"></script>
    <script src="/public/user/vendor/bootstrap/js/bootstrap.min.js"></script>
    <!--===============================================================================================-->
    <script src="/public/user/vendor/select2/select2.min.js"></script>
    <script>
      $(".js-select2").each(function () {
        $(this).select2({
          minimumResultsForSearch: 20,
          dropdownParent: $(this).next(".dropDownSelect2"),
        });
      });
    </script>
    <!--===============================================================================================-->
    <script src="/public/user/vendor/MagnificPopup/jquery.magnific-popup.min.js"></script>
    <!--===============================================================================================-->
    <script src="/public/user/vendor/perfect-scrollbar/perfect-scrollbar.min.js"></script>
    <script>
      $(".js-pscroll").each(function () {
        $(this).css("position", "relative");
        $(this).css("overflow", "hidden");
        var ps = new PerfectScrollbar(this, {
          wheelSpeed: 1,
          scrollingThreshold: 1000,
          wheelPropagation: false,
        });

        $(window).on("resize", function () {
          ps.update();
        });
      });
    </script>
    <!--===============================================================================================-->
    <script src="/public/user/js/cartManagemnt.js"></script>
    <script src="/public/user/js/main.js"></script>
  </body>
</html>
